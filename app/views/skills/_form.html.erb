<%= form_for(@skill) do |f| %>
  <%= render 'shared/error_messages' %>

  <div>
    <%= f.label :keyword %>
    <%= f.text_field :keyword %>
  </div>

  <%= f.submit t('helpers.submit.submit', :model=> Skill.model_name.human), class: "btn btn-small btn-primary" %>
<% end %>